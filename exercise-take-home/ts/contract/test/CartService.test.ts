import { CartService } from '../contract/CartService';

  
describe('CartService', () => {
  let cartService: Cart;

  beforeEach(() => {
    cartService = new Cart();
  });

  it('should do something', () => {
    // Arrange
    // Set up any necessary data or state for your test.

    // Act
    // Call the method you're testing.

    // Assert
    // Check that the method behaved as expected.
  });
});
